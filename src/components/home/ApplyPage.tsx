export default function ApplyPage() {
  return (
    <main className="bg-slate-50 px-4 py-16">
      <section className="mx-auto max-w-3xl rounded-xl bg-white p-6 shadow-sm">
        <p className="font-semibold uppercase tracking-wider text-[var(--accent)]">
          Admissions
        </p>

        <h1 className="mt-2 text-3xl font-bold text-[var(--primary)]">
          Apply to De La Salle Kigali
        </h1>

        <p className="mt-4 text-gray-600">
          The online application form will allow families to submit basic
          student information and required documents directly to the school.
        </p>

        <div className="mt-6 rounded-lg bg-blue-50 p-4 text-sm text-blue-950">
          Application form coming soon.
        </div>
      </section>
    </main>
  );
}